services:
  - type: web
    name: stdc-attendance-system
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /index.php
    # Environment variables (DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT)
    # should be set manually in Render Dashboard after creating MySQL service
